# Symbols the notifier watches (one per line). Edit + push to change.
# Multi-symbol meanrev basket: more signals than SPY-only while KEEPING the edge.
# Each backtested 12y (Connors RSI2, all win>=69% + profitable). SPY is best;
# SMH has the highest drawdown (~-24%). Widen for more frequency (lower quality),
# trim for fewer/cleaner. NOTE: these are correlated equity ETFs, so BUY signals
# cluster on selloff days (several at once) rather than spreading evenly.
SPY
QQQ
XLK
SMH
XLV
EEM
